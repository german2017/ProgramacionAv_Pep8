"""Creacion y seleccion de variables para modelado."""

import pandas as pd


def build_features(data: pd.DataFrame) -> pd.DataFrame:
    """Construye variables predictivas a partir del dataset limpio."""
    # TODO: Crear variables temporales, geograficas o categoricas relevantes.
    return data.copy()
