"""Limpieza y preparacion inicial de datos."""

import pandas as pd


def clean_data(data: pd.DataFrame) -> pd.DataFrame:
    """Aplica reglas de limpieza sobre el dataset."""
    # TODO: Tratar valores faltantes, duplicados, tipos de datos y outliers.
    return data.copy()
